/**
 * Web-only markdown cheatsheet button. The native editor is WYSIWYG (no raw
 * markdown is ever shown), so there's nothing to explain — render nothing.
 * The real implementation lives in `markdown-help.web.tsx`.
 */
export function MarkdownHelp() {
  return null;
}
